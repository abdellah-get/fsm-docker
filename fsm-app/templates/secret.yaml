apiVersion: v1
kind: Secret
metadata:
  name: fsm-secrets
type: Opaque
stringData:
  # Valeurs injectées via values.yaml / values-secret.yaml (jamais de secret réel dans Git)
  db_url: {{ .Values.secrets.dbUrl | quote }}
  auth_secret: {{ .Values.secrets.authSecret | quote }}
