apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fsm-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fsm-app-service
                port:
                  number: 3000
