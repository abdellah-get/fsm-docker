global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'web-admin'
    metrics_path: '/api/metrics'
    static_configs:
      # host.docker.internal permet au conteneur de joindre le port 3000 de ton hôte
      - targets: ['host.docker.internal:3000']